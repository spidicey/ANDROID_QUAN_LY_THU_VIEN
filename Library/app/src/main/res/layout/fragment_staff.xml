<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/ViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/BottomNavigation" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/BottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:background="@color/indigo"
        app:itemIconSize="32dp"
        app:itemIconTint="@color/navigation"
        app:itemTextAppearanceActive="@style/CustomTextAppearanceBottomNavigation"
        app:itemTextAppearanceInactive="@style/CustomTextAppearanceBottomNavigation"
        app:itemTextColor="@color/navigation"
        app:menu="@menu/navigation" />

</RelativeLayout>